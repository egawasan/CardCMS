# CardCMS CHANGELOG

このファイルは CardCMS の開発履歴を記録します。

---

## Version 1.7（2026-08-01 お問い合わせメール設定確認画面）

### 改善
- 管理画面に「お問い合わせメール設定」への入口を追加
- `admin/contact_settings.php` を追加
- `config/contact.php` の設定状況を管理画面から確認できるようにした
- メールアドレスは安全のため一部だけ表示する形にした
- 変更機能は付けず、まず確認専用画面として追加

### 方針
- 正式メールアドレス確定前のため、編集・保存機能は次段階で検討する
- `config/contact.php` は引き続きGitHubへ送らない

### 確認
- GitHubへ Version 1.7 の作業内容を送信
- さくらテスト環境へ `admin/contact_settings.php`、`admin/index.php`、`admin/card_list.php` を反映
- さくら側で未ログイン時に管理画面がログイン画面へ戻ることを確認
- さくら側で公開ページが `200 OK` で開くことを確認
- さくら側でテスト版の `noindex, nofollow` が維持されていることを確認
- さくら側で `database/cards.json` が外部から直接見えないことを確認

### 追加資料
- `docs/013_本番公開前チェックリスト.md` を作成
- 本番公開前に確認する表示、管理画面、メール、セキュリティ、SEO項目を整理

### 未確定
- 正式な送信先、BCC、送信元、返信先メールアドレスは先方確認待ち

---

## Version 1.6（2026-07-24 ヘッダーメニュー追加）

### 改善
- 公開ページのヘッダー右端にハンバーガーメニューを追加
- メニュー項目として、ホーム、事業内容、本社・工場、会社概要、お問い合わせを追加
- メニュー開閉用のJavaScriptを追加
- メニュー外クリック、リンククリック、Escキーで閉じるようにした
- スマホ表示でロゴとメニューボタンが重なりにくいように調整
- フッター表示を `CardCMS Version 1.6` に更新

### 方針
- 公開ページには管理画面へのリンクを表示しない
- 下部固定メニューはそのまま残す

### 確認
- ローカル環境でトップページとお問い合わせページの生成を確認
- さくらテスト環境でヘッダーメニューと `CardCMS Version 1.6` の表示を確認
- テスト版の `noindex, nofollow` が維持されていることを確認

### 追加整理（2026-08-01）
- 不要な初期テストカード `000001` と `000003` を削除
- 残りの公開カード11件で `cards.json` が正常に読めることを確認

---

## Version 1.5（2026-07-22 現状記録）

### 改善
- 現行サイトを参考に、お問い合わせフォームを追加
- `public/contact.php` を追加
- お名前、電話番号、住所、メールアドレス、お問い合わせ内容の5項目を追加
- 5項目を必須入力としてチェックするようにした
- メールアドレス形式をチェックするようにした
- 入力画面、確認画面、送信完了画面を追加
- メール送信処理の土台を追加
- 管理者宛メールの件名を設定できるようにした
- 管理者宛メールにBCCを設定できるようにした
- お客様宛の自動返信メールを追加
- 自動返信メールの件名と本文を設定できるようにした
- 送信先メール未設定の場合は送信せず、エラー表示するようにした
- `config/contact.example.php` を追加
- `config/contact.php` をGit管理から外す方針に変更
- テスト版がGoogle検索に登録されないように `noindex` を追加
- 下部固定メニューの「メール」をお問い合わせフォームへリンク
- お問い合わせカードからフォーム画面へ移動できるボタンを追加
- `docs/012_お問い合わせフォーム.md` を作成
- フッター表示を `CardCMS Version 1.5` に更新

### 確認
- フォーム画面が表示されることを確認
- 空送信時に入力チェックが表示されることを確認
- 正常入力時に確認画面へ進むことを確認
- 送信先メール未設定時に送信せずエラー表示されることを確認
- さくらテスト環境で管理者宛メールが届くことを確認
- さくらテスト環境でBCC受信できることを確認

### 未確定
- 正式な送信先、BCC、送信元、返信先メールアドレスは先方確認待ち

---

## Version 1.4（2026-07-18〜2026-07-21 テストアップ確認済み）

### 改善
- 公開ページのカード読み込みをPHP側へ変更
- `database/cards.json` をブラウザから直接読まない構成へ変更
- public側へ渡すカード情報を、表示に必要な項目だけに整理
- `database` フォルダへ直接アクセス拒否用の `.htaccess` を追加
- さくらアップロード前チェックリストを作成
- admin画面のログイン保護を追加
- apiにもログイン保護を追加
- ログイン画面とログアウト機能を追加
- `config/auth.php` をGit管理から外す方針に変更
- ローカル専用の初期設定ページを追加
- `config` フォルダへ直接アクセス拒否用の `.htaccess` を追加
- `www/cardcms-test/` 用のアップロード構成を整理
- テスト用入口としてルート `index.php` を追加
- ルート `.htaccess` でフォルダ一覧表示を防止
- `includes` フォルダへ直接アクセス拒否用の `.htaccess` を追加
- 管理画面のシステム情報にPHP判定を追加
- CardCMS表示バージョンを Version 1.4 に更新
- テストアップロード用フォルダとZIPを作成

### 完了
- ローカルで管理画面へのログインとログアウトを確認
- `config/auth.php` がGitHubへ送られないことを確認
- アップロード用フォルダのPHP構文、認証設定、カードデータを確認
- さくらサーバーの `www/cardcms-test/` へテストアップロード
- テストURLで公開ページと管理画面ログインを確認
- 未ログイン時に管理画面がログイン画面へ戻ることを確認
- `database/cards.json` が外部から見えないことを確認
- `config/auth.php` が外部から見えないことを確認
- Version 1.4 を「テストアップ確認済み」として区切った

---

## Version 1.3（2026-07-14〜2026-07-17 完成）

### 改善
- WordPressトップページの構成を確認し、移植メモを作成
- 公開ページのヘッダーに市場工芸のロゴを表示
- 公開ページにメイン画像を表示
- トップ紹介文を固定HTMLとして追加
- 公開ページの見出しを「カード一覧」から「お知らせ」へ変更
- 下部固定メニューの下準備として「本社・工場」「メール」「電話」を追加
- 固定メニューのリンク先として、本社・工場とお問い合わせの簡易欄を追加
- ヘッダー、メイン画像、本文、フッター、固定メニューの横幅基準を共通化
- カード番号を内部管理用とし、カード種別とレイアウトで表示用途を判断する方針を追加
- `card_type`、`layout`、`slug`、`created_at`、`updated_at` を含むカードJSON形式を設計書に追加
- 新規カード画面、編集画面、一覧画面にカード種別とレイアウトを追加
- 保存処理と更新処理で `card_type` と `layout` を扱えるようにした
- 新規カード画面、編集画面、一覧画面に管理名（slug）を追加
- 保存処理と更新処理で `slug` を扱えるようにした
- 新規保存時に `created_at` と `updated_at` を自動保存するようにした
- 編集保存時に `updated_at` を自動更新するようにした
- 編集画面と一覧画面で日時を確認できるようにした
- 公開ページで `layout` が `Text` のカードは画像枠を表示しないようにした
- 既存カードデータを `card_type`、`layout`、`slug`、`created_at`、`updated_at` を含む形式へ整理
- 既存カードの表示順を `10 / 20 / 30` に整理
- 公開ページで `ImageLeft`、`ImageRight`、`Gallery`、`Text` の基本表示スタイルを追加
- トップページを構成する主要情報ブロックのカード設計表を作成
- 固定HTMLだったメイン画像を `main-visual` のHeroカードへ移行
- `top-intro` カードを正式な紹介文カードとして更新
- `house-cleaning` カードを追加
- `main-visual` は本文を空にし、画像だけのHeroカードとして表示
- `house-cleaning` カードに代表画像と本文を設定
- `infection-control` カードを追加
- ImageLeft / ImageRight の画像を切れにくい表示へ調整
- 公開ページのカードを左右横並びではなく、上から下へ縦に積む表示へ調整
- 通常カードはタイトル・本文を先に表示し、その下に画像を表示する形へ変更
- 公開ページの本社・工場、お問い合わせの固定HTMLを外し、カード表示へ移行
- 残りのトップページ主要カードを追加
- 固定メニューのリンク先を、本社・工場カードとお問い合わせカードへ変更
- 公開ページのカード本文、見出し、行間、余白を読みやすく調整
- カードに追加画像一覧 `images` を持たせる方針を追加
- 公開ページで代表画像と追加画像を縦に並べて表示できるようにした
- 新規カード画面と編集画面に追加画像入力欄を追加
- 制作・施工・点検、保守点検についてのカードに追加画像を設定
- 管理画面の画像プレビューを外部URL画像にも対応
- 公開ページで本文を読み分け、通常文章・箇条書き・項目表示として表示できるようにした
- 新規カード画面と編集画面の本文欄に、簡単な入力ルールを表示
- 公開ページの本文、見出し、カテゴリーの文字サイズを一段大きく調整
- スマホ表示で複数画像の間隔が広く見える問題を調整
- 管理画面のカード一覧を表示順で並べるように変更
- カード一覧に「上へ」「下へ」ボタンを追加
- `move_card.php` を追加し、表示順を上下に入れ替えられるようにした
- 会社概要などの項目表示で、続き行を右側に揃えて表示できるようにした
- 本社・工場カードに Googleマップリンクを追加
- 本文内のURLをクリックできるリンクとして表示できるようにした
- 元サイト内の地図2箇所は、同じ本社・工場のGoogleマップとして本社・工場カードに集約する方針にした
- 文章部分に薄い背景色を追加し、カード本文を読みやすいパネル表示にした
- 紹介文カードは元サイトに近い濃い青背景と白文字の表示を試験的に追加
- カテゴリーは管理用情報として残し、public側では表示しないようにした
- 公開ページのカードタイトルを中央揃えにし、現行サイトの見出し表示に近づけた
- 現在のCardCMSカードで使用している現行サイト画像を `uploads` フォルダへ取り込んだ
- 公開ページのロゴ画像も `uploads` フォルダへ取り込んだ
- `cards.json` の画像指定を外部URLからローカル画像ファイル名へ変更
- 画像管理画面で追加画像も使用中として判定できるようにした
- 紹介文カードは管理用タイトルを残しつつ、public側ではタイトルを表示しないようにした
- スマホ表示で複数画像の間隔をさらに詰め、画像同士が自然に続くように調整

### 完了
- WordPressトップページ移植の第一段階として、ロゴ・メイン画像・トップ紹介文を表示できるようになった
- スマホでも使いやすい固定メニューの土台ができた
- カードを積み重ねてページを作るCardCMSらしい設計方針が明確になった
- 管理画面からカードの用途と表示方法を指定する土台ができた
- カードに管理用の識別名を持たせる土台ができた
- カードの作成日時と更新日時を記録できるようになった
- 画像なしの文章カードを自然に表示する第一段階ができた
- 既存カードデータの形式をそろえ、次のレイアウト表示調整に進みやすくなった
- レイアウトごとにカードの見た目を変える第一段階ができた
- WordPressトップページの主要ブロックをCardCMSのカードとして整理できた
- トップページの先頭3ブロックをカードとして扱う形にできた
- メイン画像と紹介文の役割をカード単位で分けられた
- ハウスクリーニングと感染症対策をサービスカードとして表示できるようになった
- 元サイトのように、カードを縦に積み重ねてトップページを作る方向へ表示を修正できた
- トップページの主要ブロックを、ひとまずすべてカードとして並べられる状態になった
- 文字まわりの第一段階調整により、公開ページのカード本文が読みやすくなった
- 複数画像を持つカードを表示できる第一段階ができた
- 本文欄を複雑にせず、入力内容に合わせて表示を整える土台ができた
- 現在の公開ページに近い読みやすい文字サイズへ調整できた
- 複数画像がスマホでも詰まりよく縦に並ぶようになった
- 管理画面からカードを上へ下へ積み替えられるようになった
- スペースで揃えなくても、項目名と内容が自然に並ぶ表示になった
- 地図は埋め込みではなく、まずは「Googleマップで見る」リンクとして扱えるようになった
- 同じ地図を2回出さず、カード単位で地図リンクを管理する形に整理できた
- 文章部分の背景色と文字色をCSSだけで調整できる第一段階ができた
- 公開ページはカテゴリー名を出さず、元サイトに近い見た目へ整理できた
- カードタイトルの位置が整い、トップページ全体の見た目が現行サイトに近づいた
- WordPress側の画像URLに依存せず、CardCMS内の画像で公開ページを表示できる状態になった
- 公開ページで使用する主要画像とロゴをCardCMS内で管理できるようになった
- 現行サイトに合わせて、トップページ下の紹介文は見出しなしで表示できるようになった
- 制作・施工・点検などの複数画像カードが、スマホでも余白少なめに縦並び表示できるようになった

---

## Version 1.2（2026-07-14）

### 改善
- 公開ページを `public/index.html` から `public/index.php` へ変更
- `public/includes/header.php` を追加
- `public/includes/footer.php` を追加
- 公開ページのヘッダー、本文、フッターを分けて管理できる形に整理
- 管理画面とカード一覧の「公開ページを見る」リンクを `index.php` へ変更
- `docs/PROJECT_STATUS.md` と `docs/CardCMS作業日誌.md` を追加

### 完了
- 公開ページのPHP化と共通レイアウト化を開始できる状態になった
- ブラウザで `public/index.php` の表示を確認した

---

## Version 1.1（2026-07-13）

### 改善
- 内部IDとは別に、画面表示用のカード番号を追加
- 既存カードに `000001` から始まるカード番号を設定
- 新規カード作成画面に次のカード番号を自動表示
- 編集画面とカード一覧で実際のカード番号を表示
- カード一覧をカード番号順に表示

### 完了
- 利用者に見せる番号をカード番号として整理できるようになった

---

## Version 1.0（2026-07-13）

### 完成内容
- CardCMS の基本機能を一通り使える状態に整理
- 管理メニュー、新規カード、カード一覧、編集、削除、画像管理、システム情報、公開ページを接続
- 画面下部のバージョン表示を Version 1.0 に統一
- README.md に起動方法、主な画面、現在の機能を記録

### 完了
- CardCMS 基本版を Version 1.0 として区切った

---

## Version 0.13（2026-07-13）

### 不具合修正
- 新規カード画面と編集画面の「戻る」ボタンが動作しない問題を修正

### 完了
- 「戻る」ボタンからカード一覧へ戻れるようになった

---

## Version 0.12（2026-07-13）

### 新機能
- 画像管理画面を追加
- uploads フォルダ内の画像を一覧表示
- 画像のファイル名、サイズ、更新日を表示
- どのカードで画像が使われているかを表示
- メニュー画面から画像管理画面へ移動できるようにした

### 完了
- アップロード済み画像を管理画面から確認できるようになった

---

## Version 0.11（2026-07-13）

### 不具合修正
- 画像未登録カードの編集画面で `htmlspecialchars()` の警告が表示される問題を修正

### 完了
- 画像が未登録でも編集画面を正常に表示できるようになった

---

## Version 0.10（2026-07-12）

### 改善
- CardCMS起動.command で最初にメニュー画面を開くように変更
- メニュー画面の「新しいカード」「カード一覧」「公開ページを見る」をリンク化
- 画像管理とシステム設定は準備中として表示

### 完了
- 以前のように最初のメニュー画面から作業を始められるようになった

---

## Version 0.9（2026-07-12）

### 新機能
- カード削除の確認画面を追加
- delete_card.php による削除処理を実装
- 管理画面の削除リンクから安全に削除確認画面へ移動できるようにした

### 完了
- 管理画面からカード削除を実行できるようになった

---

## Version 0.8（2026-07-12）

### 新機能
- 公開ページにカード一覧を表示
- published が true のカードだけを表示
- sort_order と id を使って表示順を整理
- 画像があるカードは uploads フォルダの画像を表示
- 画像がないカードには「画像なし」を表示
- 管理画面から公開ページを開くリンクを追加

### 完了
- public/index.html で公開中カードを確認できるようになった
- 管理画面のカード一覧で画像列とタイトル列の表示を整理

---

## Version 0.7（2026-07-12）

### 新機能
- カード編集画面で現在の画像を表示
- 編集画面から新しい画像をアップロードできるようにした
- 画像を変更しない場合は既存画像を保持する処理を追加

### 完了
- 登録済みカードの画像差し替え機能が完成

---

## Version 0.6（2026-07-12）

### 新機能
- 新規カード登録時の画像アップロード機能を追加
- アップロード画像を uploads フォルダへ保存
- cards.json に画像ファイル名を保存
- 管理画面のカード一覧で画像を確認できるようにした

### 完了
- カードに画像を登録できるようになった

---

## Version 0.5（2026-07-12）

### 新機能
- カード編集画面を追加
- カード一覧から編集画面へ移動できるようにした
- update_card.php によるカード更新処理を追加
- delete_card.php の準備を開始
- CardCMS 起動用コマンドを調整

### 完了
- 登録済みカードの内容を編集して保存できるようになった

---

## Version 0.4（2026-07-09）

### 新機能
- cards.json の読み込み機能を実装
- 管理画面にカード一覧表示機能を追加
- 公開状態（○／×）を一覧表示

### 完了
- 保存したカードを一覧画面に表示できるようになった

---

## Version 0.3（2026-07-09）

### 新機能
- 新規カード入力画面を作成
- フォーム送信機能を実装
- cards.json への保存機能を実装

### 完了
- CardCMS 初のデータ保存に成功

---

## Version 0.2

### 新機能
- 管理画面のレイアウト作成
- 新規カード入力画面の作成

---

## Version 0.1

### 新機能
- 開発環境を構築
- Git リポジトリを作成
- Visual Studio Code を導入
- システム設計書を作成
- データベース設計書を作成
